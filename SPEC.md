# SPEC - שיפור מערכת ההוצאות

## מטרה
לתקן ולשפר את העלאת ההוצאות והתצוגה כך שיעבדו לפי תאריך אמיתי ולא רק חודש בחודשו.

## בתחום (In scope)
- **טווח תאריכים בתצוגה** - toggle "לפי חודש / טווח תאריכים" מעל בורר המחזורים. מצב טווח מציג את כל ההוצאות שבטווח כמו עמוד ההוצאות (רשימה + סכומים, בלי יעדים).
- **שדה "תאריך ההוצאה"** בטופס הוספה ידנית - אישית ומשותפת. ברירת מחדל = היום.
- **תיקון ייבוא אקסל** - למשוך תאריך אמיתי מכל שורה, לתקן באג off-by-one (timezone), ולשבץ כל הוצאה למחזור הנכון אוטומטית (אקסל של כמה חודשים מתפצל לבד).
- **תיקון איפוס הוצאות** - דיאלוג שנפתח תמיד: מה לאפס (אישיות/משותפות/הכל), של מי (שלי/סנדרה/שנינו) + אזהרה, ובאיזה טווח תאריכים.
- **"שולם ע"י"** על הוצאה משותפת (אור/סנדרה) + **חישוב התחשבנות** - מי חייב למי כמה (החלוקה נשארת 50/50).

## מחוץ לתחום (Out of scope)
- שינוי מערכת היעדים/הקרנות, עמוד האנליטיקה, או מבנה המחזורים עצמו.

## החלטות טכניות
- מיגרציה אחת מאוחדת: הוספת `expense_date` ו-`paid_by` ל-`shared_expenses`. מילוי רטרואקטיבי `expense_date = periods.start_date`.
- העבודה מול ה-DB החי דרך Supabase MCP (`schema.sql` בריפו מיושן).
- ביצוע שלב-אחר-שלב עם אישור בין שלבים (כל השלבים נוגעים ב-`expenses/page.tsx`).
- שמות UI: "לפי חודש" / "טווח תאריכים"; "תאריך ההוצאה"; "שולם ע"י"; "התחשבנות".

## שאלות פתוחות
- באג off-by-one באקסל: רצוי קובץ אקסל דוגמה אחד כדי לאמת את התיקון מול מקרה אמיתי.
- מיקום תצוגת ההתחשבנות (בעמוד ההוצאות / סקשן ייעודי) - ייקבע בשלב 5.

## קריטריוני אימות
- העלאת אקסל 3 חודשים → SELECT מוודא כל שורה במחזור הנכון ובתאריך הנכון.
- איפוס "אישיות/שנינו" → SELECT מוודא שהוצאות שני המשתמשים נמחקו, ומחוץ לטווח נשארו.
- בחירת טווח "20.3-28.5" → מוצגות רק הוצאות בטווח.
- הוצאה משותפת עם "שולם ע"י" → ההתחשבנות מציגה יתרה נכונה. `npm run build` עובר.

---

## סטטוס - 19/5/2026 (סשן הושלם)

**מה נעשה:** כל 5 הפיצ'רים נבנו, נדחפו (קומיט `11114eb`), ופרוסים בוורסל. מיגרציית DB בוצעה. ההוצאות של סנדרה (45 שורות) נמחקו לקראת העלאה מחדש.

**מצב נוכחי:** עובד. tsc נקי, build עובר, QA חי על האתר אישר את כל 5 הפיצ'רים. 0 שגיאות קונסול.

**החלטות:** טווח תאריכים = תוספת (toggle), לא תחליף. במצב טווח מוסתרים ווידג'טים חודשיים (יעדים/קרנות/טופס הוספה). איפוס אישיות של בן הזוג עובר API מאובטח (service client). "שולם ע"י" = תיוג + התחשבנות 50/50, לא משנה את חלוקת התקציב.

**הצעד הבא:** אור יבדוק העלאת אקסל אמיתי (20/5). סנדרה תעלה מחדש את האקסל שלה.

**בעיות פתוחות:** ייבוא אקסל לא נבדק חי עם קובץ אמיתי - רק 8 בדיקות יחידה ל-normalizeDate. `next-env.d.ts` עודכן אוטומטית. CLAUDE.md הגלובלי מפנה לנתיב Chrome שגוי (`C:\Users\User`).
